<UserControl x:Class="NexusWPF.View.FAT"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:NexusWPF.View"
             mc:Ignorable="d" 
             d:DesignHeight="700" d:DesignWidth="1000">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="50" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <!-- Document Selection Pane -->
        <Grid Grid.Row="0">
            <Grid.RowDefinitions>
                <RowDefinition Height="5" />
                <RowDefinition Height="*" />
                <RowDefinition Height="5" />
            </Grid.RowDefinitions>

            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>

            <TextBlock Grid.Row="1" Grid.Column="0" Text="Project" Foreground="White" HorizontalAlignment="Right" FontWeight="Bold" Padding="0,0,20,0" VerticalAlignment="Center" />
            <ComboBox Grid.Row="1" Grid.Column="1" />

            <TextBlock Grid.Row="1" Grid.Column="2" Text="Document Name" Foreground="White" HorizontalAlignment="Right" FontWeight="Bold" Padding="0,0,20,0" VerticalAlignment="Center" />
            <ComboBox Grid.Row="1" Grid.Column="3" />

        </Grid>

        <!-- Body -->
        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="4*" />
                <ColumnDefinition Width="2*" />
            </Grid.ColumnDefinitions>

            <DataGrid Grid.Row="0">

            </DataGrid>

        </Grid>
    </Grid>
</UserControl>
